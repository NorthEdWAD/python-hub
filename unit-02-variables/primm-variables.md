# **PRIMM Activity: Variables, `print()`, and f-strings**
**Topic:** Variables, `print()`, and f-strings
**Objective:** Understand how to use variables, the `print()` function, and f-strings to display formatted output in Python.

---

## **🔮 Predict**
**Instructions:** Read the code snippet below. Without running it, predict what it will output.
**Code:**
```python
name = "Alex"
age = 16
print(f"{name} is {age} years old.")
```
**Your Prediction:**
- What will this code output?
- Will it work as written, or will there be an error? If so, what kind?

---

## **▶️ Run**
**Instructions:** Run the code in your Python environment (IDLE, Replit, etc.).
**Observations:**
- What actually happened?
- Was your prediction correct? Why or why not?

---

## **🔍 Investigate**
**Instructions:** Answer the following questions based on the code and its output.
1. What does the line `name = "Alex"` do?
2. What is the purpose of the `f` before the string in the `print()` function?
3. How would you explain the role of `{name}` and `{age}` inside the f-string to a classmate?

---
## **✏️ Modify**
**Instructions:** Change the code to achieve the following:
- **Task 1:** Update the code to use your own name and age.
- **Task 2:** Add a second line of `print()` that uses an f-string to display: `"In 5 years, [name] will be [age + 5] years old."`
**Modified Code:**
```python
[Students paste their modified code here]
```

---
## **🎨 Make**
**Instructions:** Use what you’ve learned to create something new.
- Write a script that:
  1. Creates **3 variables**: `food`, `color`, and `animal` (assign them your favorite food, color, and animal).
  2. Uses **one `print()` statement with an f-string** to display all three variables in a sentence like:
     `"My favorite food is [food], my favorite color is [color], and my favorite animal is [animal]."`
**Your Code:**
```python
[Students write their original code here]
```

---
## **💡 Reflection**
**Instructions:** Answer the following in 2–3 sentences.
- What was the most challenging part of this activity?
- What is one thing you learned or clarified about variables, `print()`, or f-strings today?
- How could you use f-strings in a real-world program (e.g., a game, a quiz, or a data display)?

---
